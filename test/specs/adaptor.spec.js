'use strict';

var adaptor = source("adaptor");

describe("Cylon.Adaptors.Rapiro", function() {
  var module;
  module = new Cylon.Adaptors.Rapiro({
    connection: {
      port: "/dev/ABCD"
    }
  });
  
  it("needs tests");
});
