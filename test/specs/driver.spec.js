'use strict';

var driver = source("driver");

describe("Cylon.Drivers.Rapiro", function() {
  var module;
  module = new Cylon.Drivers.Rapiro({
    device: {
      connection: 'connect'
    }
  });

  it("needs tests");
});
